# Сырые ответы и собранные выгрузки версионируются: сессии эфемерны, а
# воспроизводимость требует, чтобы за каждой цифрой поднимался исходник.
# Не версионируется только справочник, выкачанный из API, — он восстанавливается
# вызовом `regions fetch-tree` и не тарифицируется.
regions_tree.json
